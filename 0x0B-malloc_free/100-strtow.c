#include "holberton.h"
#include <stdlib.h>

/**
 * strtow - string
 * @str: string
 *
 * Return: If str = NULL, str = "", or NULL if it fails
 */

char **strtow(char *str)
{
	if (str == NULL || str == "")
		return (NULL);
}
