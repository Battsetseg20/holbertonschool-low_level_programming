#include·"holberton.h"

/*
*
*/

int _strlen(char *s)
{
	int i, c = 0;

	for (i = 0; *(s + i) != '\0'; i++)
	{
		c++;
	}
	return (i);
}

void rev_string(char *s)
{
	int i, s0;
	char h, n;

	s0 = _strlen(s) - 1;
	i = 0;

	while (i < s0)
	{
		h = s[i];
		n = s[s0];
		s[i++] = n;
		s[s0--] = h;
	}
}
